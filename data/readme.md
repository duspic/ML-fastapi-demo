"scraped_linkedin_jobs.csv" will be created once the data is scraped via "scripts/scrape.py"

embeddings in .npy format will be created once the scraped data is embedded via "scripts/embed_job_data.py"
